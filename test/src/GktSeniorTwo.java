
public class GktSeniorTwo {

	public static void main(String[] args) {
		
		//Can binary search be used on a Linked List?
		//Yes it can be used on any kind of data structure
		
		//What sorting algorithm do you think it's most efficient for sorting an array of 10 million random integers ? You don't know any other information about these numbers.
		//Quick Sort
		
//		//Which of the following BigO notations can express the complexity of this code sample best? (consider reasonable parameters and choose the smallest one between multiple possibilities)
		
		
		//Best sorting for sorted array
		//Insertion sort
		
		
		//What is the search complexity of an unsorted linked list?
		//O(n)
		
		//Which of the following does not affect lookup efficiency in hash tables?
		//Size of elements stored in the hash table 
		
		//Benefit of Factory design pattern
		//Decouple the class from implementation using interface
		
		
	}
	
	/*
	 * int isPowerOfTwo (int n) { if(n!=0 && (n & (n-1)) == 0) { return true; }
	 * return false; }
	 * 
	 * int isPowerOfTwo2 (int n) { while ( ( (n & 1) == 0) && n > 1) n >>= 1; return
	 * (n == 1); }
	 */
	
	
}
